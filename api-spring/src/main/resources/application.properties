server.port=8080
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/dsw}
spring.datasource.username=${DB_USER:dsw}
spring.datasource.password=${DB_PASSWORD:dsw_local}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
lab.api-key=${LAB_API_KEY:lab-only-key}
